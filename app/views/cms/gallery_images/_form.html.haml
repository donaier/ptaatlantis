%hr
= simple_form_for([:cms, @gallery_image.gallery, @gallery_image], wrapper: :bootstrap, html: { class: 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :image
    = f.input :description
    = f.hidden_field :gallery_id

  .form-actions
    = f.button :submit
