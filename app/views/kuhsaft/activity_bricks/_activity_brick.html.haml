- @activities = Activity.from_now
- @atlas = @activities.where(atlas: true)
- @titan = @activities.where(titan: true)
- @artemis = @activities.where(artemis: true)

- if @atlas.any?
  .row
    .columns.large-10.large-offset-1
      %p
      %h2 Atlas
      %h3= @atlas.first.title
      %p= @atlas.first.starts_at.strftime('%d.%m - %H:%M Uhr')
      %p= @atlas.first.description
      - if @atlas.first.meeting_point.present?
        %p= "Treffpunkt: #{@atlas.first.meeting_point}"
  .row
    .columns
      - if @atlas.size > 1
        %dl.accordion{ 'data-accordion' => true }
          %dd.accordion-navigation
            = link_to 'alle Atlas Aktivitäten', '#panel1'
            .content#panel1
              - @activities.where(atlas: true)[1..-1].each do |activity|
                .activity
                  %h3= activity.title
                  %p= activity.starts_at.strftime('%d.%m - %H:%M Uhr')
                  %p= activity.description
                  - if activity.meeting_point.present?
                    %p= "Treffpunkt: #{activity.meeting_point}"

- if @titan.any?
  .row
    .columns.large-10.large-offset-1
      %p
      %h2 Titan
      %h3= @titan.first.title
      %p= @titan.first.starts_at.strftime('%d.%m - %H:%M Uhr')
      %p= @titan.first.description
      - if @titan.first.meeting_point.present?
        %p= "Treffpunkt: #{@titan.first.meeting_point}"
  .row
    .columns
      - if @titan.size > 1
        %dl.accordion{ 'data-accordion' => true }
          %dd.accordion-navigation
            = link_to 'alle Titan Aktivitäten', '#panel2'
            .content#panel2
              - @activities.where(titan: true)[1..-1].each do |activity|
                .activity
                  %h3= activity.title
                  %p= activity.starts_at.strftime('%d.%m - %H:%M Uhr')
                  %p= activity.description
                  - if activity.meeting_point.present?
                    %p= "Treffpunkt: #{activity.meeting_point}"

- if @artemis.any?
  .row
    .columns.large-10.large-offset-1
      %p
      %h2 Artemis
      %h3= @artemis.first.title
      %p= @artemis.first.starts_at.strftime('%d.%m - %H:%M Uhr')
      %p= @artemis.first.description
      - if @artemis.first.meeting_point.present?
        %p= "Treffpunkt: #{@artemis.first.meeting_point}"
  .row
    .columns
      - if @artemis.size > 1
        %dl.accordion{ 'data-accordion' => true }
          %dd.accordion-navigation
            = link_to 'alle Artemis Aktivitäten', '#panel3'
            .content#panel3
              - @activities.where(artemis: true)[1..-1].each do |activity|
                .activity
                  %h3= activity.title
                  %p= activity.starts_at.strftime('%d.%m - %H:%M Uhr')
                  %p= activity.description
                  - if activity.meeting_point.present?
                    %p= "Treffpunkt: #{activity.meeting_point}"
